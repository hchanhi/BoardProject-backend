<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">       

	
	<select id="list"  resultType="com.vo.BoardVo">
		select bno, title, content, writer, regdate, viewcnt from board
	</select>
	
	
	<insert id="write" parameterType="com.vo.BoardVo" >
		insert into board(title, writer, content) value(#{title}, #{writer}, #{content})
	</insert>
	
	<select id="view" parameterType="int" resultType="com.vo.BoardVo">
		select bno, title, content, writer, regdate, viewcnt from board where bno = #{bno}
	</select>
	
	<select id="edit" parameterType="com.vo.BoardVo">
		update board set title=#{title}, content=#{content} where bno =#{bno}
	</select>
	
</mapper>


